apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-backend-chart.fullname" . }}
  labels:
    {{- include "tiny-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tiny-backend-chart.selectorLabels" . | nindent 4 }}
